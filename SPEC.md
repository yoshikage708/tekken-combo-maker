# CCM 現行仕様・引き継ぎ資料

## 目的

プレイヤー自身が確認した鉄拳8のコンボや連携を簡単に入力し、見やすいカード画像として保存・共有できるようにする。攻略データベースや自動正誤判定は行わない。

## 作成モード

- コンボ：始動タイプ、ダメージ、難易度、コンボメモを使用する。
- 連携：成立条件と連携メモを使用する。
- 必要なモードの項目だけを表示する。

## コマンド

- 方向：鉄拳式テンキー配置。短押しは白抜き、長押しは黒矢印。ニュートラルは☆。
- 攻撃：LP、RP、LK、RK、WP、WKと各種同時押し。
- 前ステ／バクステ：前後関係を付けた重なり矢印と名称を表示する。
- 区切り：`>`をユーザーが任意に入力する。
- スライド入力：`【`と`】`を任意に入力する。
- 入力状態、状態タグ、壁・ステージ、システム、キャラクター固有構えを使用できる。

## 編集

- 入力確認または完成カードのコマンドを選択して編集する。
- 回数、技注釈、注釈番号を後付けできる。
- 注釈番号は単体または複数コマンドの範囲へ付けられる。
- 挿入は「挿入完了」を押すまで連続し、追加位置も後ろへ進む。
- 選択削除と末尾削除を区別する。
- 区切り`>`も挿入・削除対象として選択できる。

## メモ

- 自由文へ方向、攻撃、使用中の注釈番号をインライン挿入できる。
- 内部表記 `[[ccm:type:label]]` は保存互換のため維持する。
- メモ全消去は確認後に実行する。キャラクター変更では自動消去しない。

## レシピ保存

- 保存先はlocalStorage。
- レシピキー：`ccm-recipes-v1`
- メインキャラクターキー：`ccm-main-character-v1`
- レシピオブジェクトは `schemaVersion: 1` を持つ。
- schemaVersionを持たない既存レシピもバージョン1として読み込む。
- `footnote`、`footnoteGroup`、メモ記法の互換性を維持する。

## 画像保存

- 1200×630のカード画像を生成する。
- コンボ／連携でカード種別と色を切り替える。
- 画面と同じ方向・攻撃・状態・注釈表示を描画する。
- 長いレシピは難易度・メモ欄へ重ならない倍率へ自動縮小する。
- iPhone／Androidでは共有APIを優先し、非対応時はダウンロードへ戻す。

## 公開方針

- 無料、広告なし、原則ユーザー登録なし。
- 個人情報を原則収集しない。
- 非公式ファンメイドツールとして公開する。
- 公式ロゴや公式キャラクター画像を転載しない。
- 作成画像は利用者が動画・配信・SNS・攻略共有等へ自由に使用できる。

## 今後

- Phase 3：動画制作者向けレイアウト、透過PNG、表示項目切替等を需要確認後に検討。
- Phase 4：日本語／英語切り替え。
- 機能追加時は `tests/static-check.ps1` と `tests/MANUAL_CHECKLIST.md` を基準に回帰確認する。
# Phase 3先行実装：方向入力の表示方式

- 「方向キー」「テンキー」をワンタップで切り替えられる。
- 内部の方向入力IDと保存レシピは共通で、切り替わるのは表示だけ。
- 入力確認、完成カード、保存PNGへ同じ表示計画を反映する。
- テンキー表示でもニュートラルは☆、前ステは66、バクステは44で表示する。
- テンキー数字は囲わず、短押しは白、長押しは黄色で区別する。
- テンキー表示では攻撃もLP、RP、WK、RP+RK等の文字で表示し、WP／WKはそのまま維持する。
- 各入力は独立データのまま、同一フォントの文字要素として空白なく配置する。
- 方向キー表示・テンキー表示とも、`>`までを基本の折り返し単位とし、一区間が最大幅を超える場合のみ入力単位で安全に折り返す。
- 同時押し、×回数、技注釈、注釈番号は対象入力と分割しない。
- 最後に選択した方式は端末内へ保存し、次回起動時にも維持する。
